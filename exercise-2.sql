-- Exercise 2: Filtering Data with WHERE
-- Question:
-- You have the same employees table.
-- Write a query to retrieve all employees who earn more than 55,000.

SELECT * 
FROM employees
WHERE salary > 55,000;
