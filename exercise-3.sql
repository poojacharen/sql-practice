-- Exercise 3: Sorting Data with ORDER BY
-- Question:
-- Write an SQL query to retrieve all employees' first names, last names, and salaries, sorted in descending order of salary.

SELECT *
FROM employees
ORDER BY salary DESC
